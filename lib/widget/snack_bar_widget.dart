import 'package:flutter/material.dart';

class SnackBarWidget extends StatelessWidget {
  SnackBarAction snackBarAction;
  String textContent;


  SnackBarWidget(this.textContent);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}